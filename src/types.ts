export enum Principle {
  SquashNStretch,
  Anticipation,
  FollowThrough,
  Timing
}

export enum NavSection {
  Principles,
  Software,
  Demos
}

export enum SoftwareImplementation {
  EeaseDuration,
  Spring
}

export enum Easing {
  EaseIn = "ease-in",
  EaseOut = "ease-out",
  EaseInOut = "ease-in-out"
}

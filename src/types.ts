export enum Principle {
  SquashNStretch,
  Anticipation,
  FollowThrough,
  Timing
}

export enum SoftwareImplementation {
  Keyframes,
  Spring
}

export enum Easing {
  EaseIn = "ease in",
  EaseOut = "ease out",
  EaseInOut = "ease in-out"
}
